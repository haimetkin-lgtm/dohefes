# ביקורת קטלוג מוצרים ותמחור

מסמך תכנון/audit בלבד. **לא בוצע שום שינוי קוד או תוכן באתר.** נבדקו בפועל, קריאת קוד מלאה (לא
הונח מראש): `app/page.tsx`, `app/start/page.tsx`, `app/calculator/page.tsx` (+`ReportView.tsx`),
`app/report/page.tsx`, `app/tracking/page.tsx`, `app/ranking/page.tsx`, `app/ranking-sample/page.tsx`,
`app/custom/page.tsx`, `app/custom/intake/page.tsx`, `app/components/ConsultationCTA.tsx`, קישורי
Cardcom הקיימים (`NEXT_PUBLIC_CARDCOM_LINK_*`), `lib/report/exportExcel.ts`/`exportTrackingExcel.ts`/`exportRankingExcel.ts`,
`supabase/schema.sql`, `supabase/functions/_shared/payment-products.ts`, וכל מסמכי ה-`.md` הקיימים
בשורש הריפו.

---

## ⚠️ החלטות עסקיות סופיות (2026-08-29) - מתועדות, טרם מיושמות

**כל ההחלטות שלמטה סופיות.** המסמך כולו עודכן בהתאם. **שום קוד, סכמה, Edge Function, תשלום או
הרשאה לא שונה בפועל** - זה עדיין מסמך תכנון/audit בלבד.

### קטלוג המוצרים

שלושה מוצרים נפרדים בתשלום, **כולם 980 ₪**, **כולם דרך אותו ממשק ומנגנון תשלום של Cardcom** (אין
דף תשלום נפרד לכל מוצר) - אך **כל רכישה נשמרת עם `productType` נפרד**, כדי שהרשאה תהיה ספציפית
למוצר שנרכש בלבד, לא גורפת:

| `productType` | שם תצוגה | מחיר |
|---|---|---|
| `baseReport` | דוח אפס | 980 ₪ |
| `cashFlowAnalysis` | ניתוח תזרים ומימון מתקדם | 980 ₪ |
| `trackingReports` (חדש, שם מוצע) | דוחות מעקב בנייה | 980 ₪ |

### דוחות מעקב - **אינם כלולים עוד** ברכישת דוח אפס

מוצר המשך **אופציונלי**, נפרד, 980 ₪. **כל ההבטחות הקיימות באתר שהמעקב "כלול, ללא תשלום נוסף"
יוסרו בעתיד** ויוחלפו בהצגה ברורה כמוצר המשך בתשלום. הגישה ל-`/tracking` תדרוש **בעתיד**
entitlement מסוג `trackingReports`, מקושר ל-`reportId` הספציפי - **לא מיושם בסבב הזה**, ר' סעיף 7
לסדר הביצוע.

### אין לקוחות קיימים להחריג

**האתר חדש - אין רכישות היסטוריות לשמר זכאות עבורן.** אין צורך ב-grandfathering, אין מיגרציית
זכאויות רטרואקטיבית, ו**אין להמציא נתוני רכישה קיימים** בשום שלב של היישום העתידי. זה מבטל את
ההצעה שהופיעה בגרסה הקודמת של מסמך זה (שמירת זכאות לרכישות "ישנות") - **אינה רלוונטית עוד**.

### דירוג דירות - כלול, לא מוצר עצמאי

כלי דירוג הדירות **כלול ללא תשלום נוסף בדוח פינוי-בינוי** - **אינו** מוצר עצמאי בתשלום, **אינו**
דורש `productType` נפרד משלו. ביישום העתידי: גישה לכלי מתוך דוח פינוי-בינוי **מלא שנרכש** (לא
חופשי כמו היום), מקושרת ל-`reportId`, **לא** מוצגת כתוספת בתשלום, **לא** מוצגת עבור סוגי עסקה
לא-רלוונטיים (כלומר לא `pinuyBinui`) - ועדיין נשמרת אפשרות לדף דוגמה נפרד (`ranking-sample`)
שאינו קשור לשום דוח לקוח אמיתי.

### גרסת הבדיקה החינמית - "טיוטה להתרשמות" מפורש, לא ייצוא חינמי

המחשבון החינמי הוא **גרסת בדיקה בלבד** - יסומן ככזה **במפורש ובאופן בולט**. עדיין ניתן להזין
נתונים ולצפות בתוצאות. **כפתורי Excel/הדפסה נשארים מוצגים** (כדי שהמשתמש יידע שהם קיימים במוצר),
אך **נעולים** - לא מבצעים ייצוא/הדפסה בפועל; ליד/בלחיצה עליהם מוצגת הודעה: *"ייצוא Excel והפקת
דוח להדפסה זמינים בגרסה המלאה בתשלום"*. **אין לאפשר עקיפה** דרך פונקציית ייצוא אחרת במסך. בדוח
מלא שנרכש (`reportId` קיים) - Excel והדפסה/PDF **פעילים** כרגיל.

**כל הסעיפים למטה (2 ואילך) עודכנו כדי לשקף את ההחלטות האלה - חלקם עדיין מתעדים את המצב הקיים
בפועל (שטרם השתנה), עם ציון מפורש מה ישתנה וכיצד, לפי סדר הביצוע בסעיף 7.**

---

## 1. כל המוצרים והכלים שהאתר מציג כיום

| # | שם | סוג | חשוף לציבור היום? |
|---|---|---|---|
| 1 | דוח אפס בסיסי (`baseReport`) | מוצר בתשלום, 980 ₪ | כן, `/start` |
| 2 | דוח אפס בהתאמה אישית (`custom`) | מוצר בתשלום, 1,800 ₪ | כן, `/custom` |
| 3 | שיחת ייעוץ עם חיים אטקין | מוצר בתשלום, 1,180 ₪ | כן, בתוך כל תצוגת דוח (`ReportView`) |
| 4 | דוחות מעקב בנייה (`trackingReports`, שם חדש מוצע) | **מוצר בתשלום סופי, 980 ₪ - החל מרכישה חדשה** | כן, `/tracking`, **עדיין ללא שער תשלום בפועל (טרם יושם)** |
| 5 | ניתוח תזרים ומימון מתקדם (`cashFlowAnalysis`) | מוצר בתשלום, תשתית קיימת בקוד, 980 ₪ | **לא** - אין קישור באתר החי כלל (`app/cashflow` קיים רק בענף `gen2-cashflow-ui-implementation`, לא ממוזג) |
| 6 | כלי דירוג יחידות (`ranking`) | **כלול, לא מוצר עצמאי** - כלול ללא תשלום נוסף בדוח פינוי-בינוי **מלא שנרכש** (סופי) | כן, `/ranking` + `/ranking-sample` - **פתוח היום ללא תלות ברכישה, ישונה** |
| 7 | ייצוא Excel (דוח כדאיות) | תכונה בתוך #1/#2 - 🔴 בגרסת בדיקה: כפתור מוצג אך נעול (סופי, ר' הממצא למעלה) | כן היום, ללא שער - **ישונה לנעילה בטיוטה** |
| 8 | ייצוא Excel (מעקב בנייה) | תכונה בתוך #4 | כן, ללא שער נפרד |
| 9 | ייצוא Excel (דירוג יחידות) | תכונה בתוך #6 | כן, ללא שער נפרד |
| 10 | הדפסה/PDF (דוח כדאיות ומעקב) | תכונה, `window.print()` - 🔴 אותה נעילה בטיוטה כמו Excel | כן, ללא שער נפרד - **ישונה לנעילה בטיוטה** |

---

## 2. לפי מקום: הבטחה, מחיר מוצג, תכולה מובטחת, ומה הקוד באמת מאפשר

### `app/page.tsx` (עמוד הבית)
- **מבטיח**: דוח אפס 980 ₪ כולל מעקב חינם; דוח בהתאמה אישית 1,800 ₪; "כולל טבלאות דירוג למיזמי
  פינוי בינוי" (בכרטיס דוח עצמאי).
- **מחיר מוצג**: רק בטקסט חופשי בתוך ה-FAQ (`980 ₪`/`1,800 ₪`) - **לא** מיובא מ-`BASIC_PRICE_NIS`/`CUSTOM_PRICE_NIS`
  (`lib/supabase.ts`) - מספרים כתובים ידנית, מקור נפרד מהמקומות שבפועל גובים תשלום.
- **קוד בפועל**: עמוד סטטי לגמרי, שני קישורי CTA (`/start`, `/custom`) - אין שום לוגיקת תשלום/גישה
  כאן.

### `app/start/page.tsx` (רכישת `baseReport`)
- **מבטיח**: "כלול במחיר" - מילוי נתונים, דוח מלא (Excel+PDF), **וכל דוחות המעקב, ללא תשלום נוסף**.
  לכל סוג עסקה - תיאור קצר; ל-`pinuyBinui` בלבד - הבטחה ספציפית: *"כולל טבלאות לדירוג דירות עם
  אפשרות לקביעת הפרמטרים לדירוג"* + קישור לדוגמה.
- **מחיר מוצג**: `BASIC_PRICE_NIS` (980 ₪) - **כן** מיובא מ-`lib/supabase.ts`, מקור אחד עקבי כאן.
- **קוד בפועל**: `handlePay` בונה קישור ל-`NEXT_PUBLIC_CARDCOM_LINK_BASIC` עם `SuccessRedirectUrl`
  שמצביע ל-`/calculator/?paid=true&dealType=X`. **זה המודל הישן, הבלתי-מאומת** - `?paid=true` הוא
  אות מהדפדפן בלבד, לא הוכחת תשלום (ר' `GEN2_PAYMENT_ENTITLEMENT_DESIGN.md` §2.3/§2.5 - ממצא
  ביקורת קודם, לא חדש). **התשתית המאובטחת החדשה (`dohefes-create-payment-order` וכו') כבר קיימת
  ופרוסה בפועל בשרת (כולל `baseReport` ברשימת המוצרים!) - אך `/start` עדיין לא הועבר להשתמש בה.**
  זה פער אמיתי בין מה שנבנה למה שבאמת פעיל היום, לא חדש לביקורת הזו אך רלוונטי לתעד כאן במפורש.
- **דירוג יחידות**: הקישור ל-`/ranking-sample/` מוצג רק כש-`dealType===pinuyBinui` נבחר - אבל
  **הכלי עצמו (`/ranking/`) חופשי לגמרי היום, גם לפני כל תשלום** (ר' ניתוח `/calculator` למטה) -
  ההבטחה "כלול" כאן **תואמת את ההחלטה הסופית** (כלול בדוח פינוי-בינוי, לא מוצר נפרד), אך ביישום
  העתידי `/ranking-sample/` ימשיך לשמש דוגמה חופשית כפי שהיא היום, בעוד `/ranking/` עצמו (הכלי
  האמיתי, לא הדוגמה) יידרש להיות מקושר לדוח פינוי-בינוי **שנרכש בפועל**, לא זמין סתם למי שבחר
  את סוג העסקה בטופס בלי לשלם - ר' סעיף 7.

### `app/calculator/page.tsx`
- **מבטיח**: "גרסת בדיקה: התוצאה מוצגת מיד, ללא תשלום. לשמירת הדוח וקבלת קישור קבוע, יש לרכוש".
- **🔴 ממצא בעדיפות גבוהה, החלטה סופית התקבלה**: **המחשבון המלא, כולל הורדת Excel והדפסת PDF,
  זמינים לחלוטין בחינם היום**, בלי `reportId`, בלי תשלום, בלי הגבלה (`downloadWorkbook`/`window.print()`,
  שורות 1074-1085, **מחוץ** לכל תנאי `reportId`). מי שמוריד Excel/PDF פעם אחת ולא שומר קישור -
  קיבל בפועל תוצר מקצועי מלא בלי שום סימן "טיוטה"/"דוגמה" עליו.
  **הוכרע**: גרסת הבדיקה תסומן במפורש ובאופן בולט כ"טיוטה להתרשמות". כפתורי Excel/הדפסה **יישארו
  מוצגים** (כדי שהמשתמש יידע שהם קיימים במוצר) אך **יינעלו** - לחיצה עליהם (או ליד הכפתור) תציג
  הודעה: *"ייצוא Excel והפקת דוח להדפסה זמינים בגרסה המלאה בתשלום"* - **בלי לבצע ייצוא/הדפסה
  בפועל, ובלי עקיפה** דרך מסלול ייצוא אחר במסך. בדוח מלא שנרכש (`reportId` קיים) - הכפתורים
  פעילים כרגיל, בלי שינוי. **טרם מיושם** - ר' סעיף 7 לסדר הביצוע.
- **`payment_status:"paid"` נכתב ישירות מהדפדפן** (שורה 336) על סמך `?paid=true` בלבד - שוב, המודל
  הישן הבלתי-מאומת, לא ה-entitlement המאובטח החדש.
- **קישור למעקב**: מוצג **רק** לפי `reportId` (כלומר: כל דוח ששולם/נשמר) - שום שער נוסף היום.
  **יזדקק לשער `trackingReports` נפרד** (ר' סעיף 7) - עצם קיום `reportId` לדוח הבסיס כבר לא יספיק.
- **כלי דירוג**: מוצג (`target="_blank"`) בכל פעם ש-`dealType==="pinuyBinui"`, **ללא שום תלות
  ב-`reportId`/תשלום** - כלומר, נגיש גם ב"גרסת בדיקה" החינמית לחלוטין, לפני כל רכישה. **הוכרע
  שזה ישתנה**: הקישור יידרש `reportId` של דוח פינוי-בינוי **מלא שנרכש** - לא יוצג כלל לפני כן,
  ולא לסוגי עסקה אחרים (ר' סעיף 7).

### `app/report/page.tsx`
- אין הבטחות מחיר (עמוד צפייה בדוח קיים בלבד). קישור למעקב - זהה לחלוטין ל-`/calculator`: מוצג
  תמיד, ללא שער נוסף.

### `app/tracking/page.tsx`
- **אין שום התייחסות לתשלום בקובץ הזה בכלל** - לא `payment_status`, לא מחיר, לא בדיקת הרשאה
  מעבר לכך שיש `reportId` תקין. **זה בדיוק הקוד שיזדקק לשער entitlement מסוג `trackingReports`**
  (ר' ההחלטות הסופיות למעלה + סעיף 7 לסדר הביצוע) - `reportId` לבדו כבר לא יספיק.

### `app/custom/page.tsx` + `app/custom/intake/page.tsx`
- **מבטיח**: תשלום → טופס+קבצים → סוכן חכם בונה שלד. **לא מוזכרת בכלל תכולת מעקב** (בשונה
  מ-`/start`, שמפרטת זאת ליד כפתור התשלום) - **חוסר עקביות בגילוי מידע**, לא סתירה טכנית: מבחינת
  הקוד, ברגע שנוצר `dohefes_reports` (בהמשך, דרך תהליך הסוכן החכם ב-insure-vda - לא בריפו הזה),
  אותו דוח מקבל בדיוק אותה גישה חופשית ל-`tracking` כמו כל דוח אחר (העמודה משותפת, לא מותנית
  במקור היצירה). הלקוח **כן** מקבל את זה בפועל, רק שאף אחד לא הבטיח לו את זה מראש.
- **מחיר מוצג**: `CUSTOM_PRICE_NIS` (1,800 ₪), מיובא נכון מ-`lib/supabase.ts`.
- **הזמנה נכתבת לטבלה נפרדת** (`dohefes_custom_orders`, לא `dohefes_reports` ישירות) - `insert`
  ב-`custom/intake/page.tsx:50-52`, לא נבדק כאן לעומק (מחוץ לרשימת הקבצים המבוקשת), אך אושר
  שאין שם התייחסות ל-`tracking`/מעקב כלל.

### `app/components/ConsultationCTA.tsx`
- מוצג בתוך `ReportView` - כלומר, **בכל תצוגת דוח, כולל בגרסת הבדיקה החינמית** לפני כל תשלום.
  1,180 ₪, קישור Cardcom נפרד, **ללא שום קשר קוד ל-`reportId`/`dohefes_reports`** - עסקה עצמאית
  לגמרי, מאושרת ידנית ב-WhatsApp, לא נכתבת למסד הנתונים בכלל.

### `app/ranking/page.tsx` + `app/ranking-sample/page.tsx`
- **קוד בפועל, מצב היום**: **כלי עצמאי לחלוטין** - אין בו שום ייבוא של Supabase, שום `reportId`,
  שום בדיקת תשלום. נגיש בכתובת ישירה לכל מבקר באתר, בלי שום תלות ב-`/calculator` או ברכישה
  כלשהי. `ranking-sample` הוא אותו כלי בדיוק עם נתוני-קבע לדוגמה.
- **אין שום הבטחת מחיר על העמוד עצמו** - המחירים המוצגים בטבלה (`מחיר בסיס`, `מחיר מתואם`) הם
  קלט/פלט של החישוב עצמו (שווי דירות), **לא** מחיר של הכלי.
- **הוכרע**: `/ranking/` **לא** הופך למוצר בתשלום - נשאר כלול בדוח פינוי-בינוי. אבל הגישה אליו
  תשתנה: יידרש `reportId` של דוח פינוי-בינוי **מלא שנרכש** (לא רק `dealType===pinuyBinui` שנבחר
  בטופס, כמו היום), לא יוצג/יידרש עבור סוגי עסקה אחרים, ולעולם לא כ"תוספת בתשלום" בניסוח.
  `ranking-sample` **נשאר כפי שהוא** - דף דוגמה נפרד, לא קשור לשום דוח לקוח אמיתי, ללא צורך
  ברכישה. **טרם מיושם** - ר' סעיף 7.

### קישורי התשלום הקיימים (`NEXT_PUBLIC_CARDCOM_LINK_*`)
שלושה קישורים קבועים, כולם במודל הישן (Cardcom hosted-link, `SuccessRedirectUrl`/`FailedRedirectUrl`,
ללא אימות שרת אמיתי): `_BASIC` (`/start`), `_CUSTOM` (`/custom`), `_CONSULTATION` (`ConsultationCTA`).
**אין קישור קבוע/משתנה סביבה עבור `cashFlowAnalysis`** - התשתית שנבנתה לו (ר' סבבים קודמים) היא
כולה מהסוג המאובטח החדש (`dohefes-create-payment-order`), לא קישור Cardcom קבוע כלל - שינוי
ארכיטקטוני, לא רק מוצר נוסף באותה תבנית.

### ייצואי Excel/PDF
שלושה ייצואים עצמאיים (`exportExcel.ts`/`exportTrackingExcel.ts`/`exportRankingExcel.ts`) - אף
אחד מהם לא כולל בדיקת הרשאה/תשלום כלשהי בקוד עצמו (מקבלים נתונים מוכנים, מייצרים קובץ) - ההגנה
היחידה (אם יש) היא ברמת **מי שקורא להם**, וראינו שברוב המקומות (`calculator`, `ranking`) אין הגנה
כזו בכלל.

### מסמכי התכנון הקיימים
- `GEN2_PAYMENT_ENTITLEMENT_DESIGN.md`: מסמך ה-audit שזיהה את חולשת "אמון בלקוח" המקורית -
  לא עוסק בתמחור מעקב/דירוג, רלוונטי כרקע לפער התשלום הישן/חדש שתועד למעלה.
- `GEN2_CASHFLOW_UI_DESIGN.md`: המקור הרשמי לכך ש-`cashFlowAnalysis` הוא **מוצר נפרד, entitlement
  נפרד**, 980 ₪ - "לא הרחבה כלולה" (§0.1.1) - עקבי עם `payment-products.ts`, ומשמש כתבנית ישירה
  למודל שה-entitlement החדש למעקב אמור לאמץ (ר' סעיף 7). מצטט גם במפורש שהמעקב **כלול** בדוח
  הבסיס - **מסמך נוסף שמשקף את המדיניות הישנה** ויזדקק לעדכון עקבי כשמחיר המעקב ייקבע, לא רק
  קבצי `app/`.
- `GEN2_UNIT_RANKING_DESIGN.md`: מסמך טכני-אלגוריתמי בלבד (איכות נוסחת הדירוג, לא תמחור) - **אינו
  עוסק כלל בשאלת "כלול/נפרד"** - מאשר שאין החלטה עסקית קיימת בעניין (עקבי עם מה שציינת כ"טרם
  הוחלט").
- `SECURE_PAYMENT_DEPLOYMENT_RUNBOOK.md`: תפעולי (deploy/secrets/tests), לא עוסק בתמחור/תכולה.

---

## 3. סתירות בין עמודים

**כל המקומות באתר (עמוד הבית, `/start`, `/calculator`, `/report`, מבנה הנתונים) מסכימים ביניהם
במלואם** על מדיניות המעקב - אבל כולם משקפים את **המדיניות הישנה** (כלול, בלי תשלום נוסף), שכעת
**הוחלפה סופית** בהחלטה החדשה (ר' ההחלטות הסופיות למעלה). זה לא "כמה עמודים סותרים אחד את השני" -
זה **כל האתר כאחד, טרם עודכן להחלטה העסקית**. הפער הזה הוא חוב ההתאמה שתועד למעלה, לא סתירה
פנימית לפתור בין עמודים - ומטופל כשלב שלם בתוכנית התיקון (סעיף 7, שלב 3).

סתירות/אי-עקביות אמיתיות בין עמודים (בלתי-תלויות בהחלטת המעקב) שכן נמצאו:

1. **`/custom` שותק על תכולת מעקב**, בעוד `/start` מפרט זאת במפורש - היה חוסר גילוי-לפני-תשלום
   כשמעקב היה כלול; לאחר יישום ההחלטה החדשה, שני העמודים ממילא יזדקקו לאותו תיקון (הסרת ההבטחה
   הישנה, החלפתה בהצגת המעקב כמוצר המשך בתשלום) - אותה פעולה תכסה את שניהם.
2. **ניסוח דירוג היחידות לא אחיד**: עמוד הבית אומר "כולל טבלאות דירוג למיזמי פינוי בינוי" (משתמע:
   הטבה בתוך מוצר בתשלום), בעוד בפועל היום **אף אחד לא צריך לשלם** כדי להשתמש בכלי - הניסוח
   הקיים מדויק להחלטה הסופית (כלול, לא מוצר נפרד) **אך מטעה לגבי איך זה נאכף בפועל** (משתמע
   שקיים שער שמעקף אותו רכישה, בעוד היום אין שער כלל - ולפי ההחלטה, יהיה שער שמצריך דוח
   פינוי-בינוי מלא, לא "כלול לכל דורש").
3. **מחירי עמוד הבית לא מיובאים מ-`lib/supabase.ts`** - כתובים כטקסט חופשי בתוך ה-FAQ, בעוד
   `/start` ו-`/custom` כן מייבאים קבוע. סיכון סחיפה עתידית (עדכון מחיר במקום אחד בלי השני) -
   בדיוק מה ש-`lib/catalog.ts` המוצע (סעיף 5) פותר.
4. **שני מודלי תשלום קיימים זה לצד זה** - הישן (`?paid=true`, `payment_status`, קישורי Cardcom
   קבועים) עדיין פעיל בפועל בכל שלושת המוצרים הקיימים באתר החי; החדש (entitlement מאובטח) קיים
   ופרוס בשרת, אך משמש רק תשתית ל-`cashFlowAnalysis` שעדיין לא חשוף לציבור. **הוכרע**: `trackingReports`
   ייבנה ישירות על ה-entitlement המאובטח (לא המודל הישן) - ר' סעיף 7. מיגרציית `baseReport`/`custom`
   עצמם למודל המאובטח נשארת יוזמה נפרדת, מחוץ להיקף הזה.

---

## 4. טבלה לפי מוצר

| מוצר | מחיר | תכולה מובטחת | entitlement בקוד | שער גישה בפועל היום | מצב סופי (טרם מיושם) |
|---|---|---|---|---|---|
| **דוח אפס** (`baseReport`) | 980 ₪ (FAQ: טקסט חופשי; `/start`: `BASIC_PRICE_NIS`) | מילוי נתונים, דוח מלא, Excel+PDF | קיים ב-`payment-products.ts`, Edge Functions פרוסות - **לא בשימוש בפועל** | `?paid=true` בלבד (מודל ישן, לא מאומת) | ללא שינוי בהיקף הזה (מיגרציה למאובטח = יוזמה נפרדת) |
| **דוח אפס בהתאמה אישית** (`custom`) | 1,800 ₪ | טופס+סוכן חכם בונה שלד; מעקב לא מוזכר | אין entitlement כלל, טבלה נפרדת (`dohefes_custom_orders`) | `?paid=true` בלבד | ללא שינוי בהיקף הזה |
| **שיחת ייעוץ** | 1,180 ₪ | שיחת Meet עד שעה, מוקלטת | אין - לא קשור ל-`dohefes_reports` בכלל | קישור Cardcom + אישור WhatsApp ידני | ללא שינוי |
| **דוחות מעקב** (`trackingReports`) | **980 ₪ - סופי** | תקציב מול ביצוע, לאורך הבנייה | **אין עדיין - נדרש `ProductType` שלישי** | **אין שום שער עדיין** - רק `reportId` תקין; האתר עדיין מבטיח "ללא תשלום נוסף" | מוצר המשך בתשלום נפרד, entitlement מאובטח מקושר ל-`reportId`, כל ההבטחות הישנות מוסרות |
| **ניתוח תזרים** (`cashFlowAnalysis`) | 980 ₪ (בקוד בלבד) | תזרים חודשי מלא, נפרד מדוח הבסיס | entitlement אמיתי, נפרד, כבר בשרת | לא חשוף לציבור - אין קישור באתר | ללא שינוי בהיקף הזה |
| **דירוג יחידות** (`ranking`) | **אין - כלול, לא מוצר** | כלול בדוח פינוי-בינוי | **לא נדרש entitlement/`ProductType` - החלטה סופית** | **אין שום שער** - כלי פתוח לחלוטין, גם בלי `reportId`/רכישה | דורש `reportId` של דוח פינוי-בינוי **שנרכש**, לא מוצג לסוגי עסקה אחרים; `ranking-sample` נשאר פתוח |
| **Excel/PDF** (דוח כדאיות) | ללא מחיר נפרד | "ניתן להורדה/הדפסה" | אין | **אין שום שער** - זמין גם ב"גרסת בדיקה" חינמית | כפתורים נשארים מוצגים בטיוטה אך נעולים; פעילים רק כש-`reportId` קיים (דוח נרכש) |

---

## 5. הצעת מבנה למקור אמת מרכזי (תכנון בלבד - **לא נוצר קובץ בסבב הזה**)

מוצע מודול חדש, `lib/catalog.ts`, שכל עמוד/רכיב שמתאר מוצר/מחיר/תכולה/זכאות **יקרא ממנו**, ולא
יכתוב טקסט/מספר/תנאי בעצמו. המבנה, כולל כל השדות שהתבקשו:

```ts
// תכנון בלבד - לא נכתב, לא נוצר קובץ
export type ProductId = "baseReport" | "custom" | "consultation" | "cashFlowAnalysis" | "trackingReports";

/** מזהה מוצר של מערכת התשלום המאובטחת - תת-קבוצה של ProductId (לא כל מוצר כאן חייב תשלום
 *  מאובטח נפרד - "custom"/"consultation" למשל ממשיכים במודל הישן/עצמאי, ר' סעיף 3 פריט 4). */
export type SecurePaymentProductType = "baseReport" | "cashFlowAnalysis" | "trackingReports";

export type AllowedAction = "view" | "excel" | "print" | "tracking" | "ranking";

interface CatalogEntry {
  id: ProductId;                        // מזהה מוצר
  displayName: string;                  // שם תצוגה
  priceAgorot: number | null;           // מחיר באגורות (לא ש"ח - תואם expected_amount_agorot בשרת); null = "כלול, ללא מחיר עצמאי" (כמו ranking)
  description: string;                  // תיאור
  includedFeatures: string[];           // תכולת המוצר - טקסט תכולה קבוע, מוצג בכל עמוד שמזכיר את המוצר
  requiresReportId: boolean;            // האם דורש reportId
  relevantDealTypes: DealType[] | "all"; // סוגי העסקאות הרלוונטיים ("all" לרוב המוצרים; ["pinuyBinui"] לדירוג)
  allowedActions: AllowedAction[];       // פעולות מותרות: צפייה/Excel/הדפסה/מעקב/דירוג
  paymentProductType: SecurePaymentProductType | null; // מיפוי ל-productType של מערכת התשלום, כשרלוונטי - null אם המוצר לא עובר דרך המערכת המאובטחת (custom/consultation) או שהוא כלול-ולא-נמכר (ranking)
}
```

**דוגמה ממחישה (לא קוד סופי)** - איך `trackingReports`/`ranking` היו נראים תחת המבנה הזה:

```ts
// תכנון בלבד, להמחשה בלבד
{
  id: "trackingReports",
  displayName: "דוחות מעקב בנייה",
  priceAgorot: 98_000,
  description: "מעקב תקציב מול ביצוע בפועל, לאורך חודשי הבנייה",
  includedFeatures: ["טבלת תקציב מול ביצוע לפי שלב", "ייצוא Excel", "הדפסה/PDF"],
  requiresReportId: true,
  relevantDealTypes: "all",
  allowedActions: ["view", "excel", "print", "tracking"],
  paymentProductType: "trackingReports",
}
{
  id: "ranking", // לא ב-ProductId למעלה בכוונה - אינו "מוצר" למכירה, ר' הערה
  ...
  priceAgorot: null,
  requiresReportId: true, // (של דוח פינוי-בינוי, לא שלו עצמו)
  relevantDealTypes: ["pinuyBinui"],
  allowedActions: ["view", "excel", "ranking"],
  paymentProductType: null, // כלול - לא עובר דרך מערכת התשלום בכלל
}
```

(שני הדוגמאות ממחישות למה - "דירוג" בפועל אינו חבר ב-`ProductId`/`SecurePaymentProductType` כלל,
כי הוא לא נמכר בנפרד - ייתכן שיישאר טיפוס `CatalogFeature` נפרד מ-`CatalogEntry` ביישום בפועל,
זו החלטת מבנה-קוד קטנה שתתקבל בזמן היישום, לא כאן.)

**עקרון מרכזי**: `priceAgorot`/`includedFeatures`/`description` נקראים תמיד מכאן - `app/page.tsx`
(FAQ), `app/start/page.tsx` ("כלול במחיר"), `app/custom/page.tsx`, `app/tracking/page.tsx` - כולם
מציגים אותו טקסט/מספר, במקום גרסאות נפרדות שעלולות לסטות זו מזו (כפי שכבר קרה לגבי מחירי עמוד
הבית, ר' סעיף 3). `requiresReportId`/`relevantDealTypes`/`allowedActions`/`paymentProductType`
הם גם תשתית אכיפה (לא רק תיעוד) - הרכיבים בפועל (`/tracking`, `/ranking`, כפתורי Excel) יכולים
להתבסס עליהם ישירות במקום לשכפל את הלוגיקה. **אין הצעה ליצור את הקובץ עכשיו** - זו רק תוכנית
למבנה, ר' סעיף 7 לרצף הביצוע (שלב 1).

---

## 6. שאלות פתוחות שנותרו - לא ניתן להסיק מהקוד

רוב ההחלטות שהיו רשומות כאן הוכרעו סופית (ר' ראש המסמך). מה שעדיין פתוח, ואינו נגזר מהקוד:

1. **מתי (ואם בכלל, בשלב הזה) `/calculator`/`/report` יקבלו קישור אמיתי לרכישת `cashFlowAnalysis`?**
   האם באותו רגע גם מיקום/ניסוח `ConsultationCTA`/קישור הדירוג/קישור המעקב-בתשלום-החדש צריכים
   להשתנות יחד, כדי שהעמוד לא ייראה עמוס במדי הרבה CTAs בבת אחת?
2. **מיגרציית `baseReport`/`custom` עצמם למודל המאובטח** - נשארת מוגדרת כיוזמה נפרדת ומחוץ להיקף
   הזה (ר' סעיף 7, שלב 7) - האם/מתי לתעדף אותה נשאר פתוח.
3. **שם השדה/הטיפוס המדויק לדירוג** במבנה `lib/catalog.ts` (`CatalogFeature` נפרד מ-`CatalogEntry`,
   או הרחבה כלשהי אחרת) - החלטת מבנה-קוד קטנה, לא עסקית, שתתקבל בזמן היישום עצמו (סעיף 5).

---

## 7. תוכנית תיקון ב-commits קטנים (רק לאחר אישור לכל שלב, לא מבוצע כעת)

**הסדר הבא הוא הסדר המחייב** (לא רק "מוצע") - כל שלב מייצר בסיס לשלב שאחריו, ותלוי באישור נפרד
לפני שהוא נפתח בפועל:

1. **מקור אמת מרכזי לקטלוג המוצרים** - יצירת `lib/catalog.ts` (מבנה מלא בסעיף 5) עם הנתונים
   הסופיים: שלושת המוצרים (980 ₪ כל אחד, `productType` נפרד), דירוג כ"כלול" ללא `productType`.
   אין שינוי לשום עמוד קיים בשלב הזה - הקובץ נוצר, לא נקרא עדיין משום מקום.
2. **הוספת `trackingReports` למנגנון התשלום ולסכמת ההרשאות** -
   `supabase/functions/_shared/payment-products.ts` (`ProductType` שלישי, 980 ₪, בדיקות ייעודיות
   בדיוק כמו `cashFlowAnalysis` הקיים) + entitlement מאובטח על התשתית הקיימת כבר בשרת
   (`dohefes-create-payment-order`/`dohefes-get-product-access`/RLS) - **לא** המודל הישן
   (`?paid=true`). **אין מיגרציה רטרואקטיבית/grandfathering** - הוכרע שאין לקוחות קיימים
   להחריג (ר' ראש המסמך) - שער התשלום פשוט מופעל, בלי שלב "המרה" כלשהו.
3. **תיקון התוכן בעמוד הבית, `/start`, ושאר נקודות המכירה** - חיווט ל-`lib/catalog.ts`: הסרת
   ההבטחה "מעקב כלול, ללא תשלום נוסף" מ-`app/page.tsx` (FAQ + כרטיס) ומ-`app/start/page.tsx`
   ("כלול במחיר"), החלפתה בהצגת `trackingReports` כמוצר המשך אופציונלי, 980 ₪. `/custom` מאומת
   שאינו מבטיח מעקב חינמי (ר' סעיף 3, פריט 1).
4. **נעילת Excel והדפסה בגרסת הבדיקה** - `app/calculator/page.tsx`: תיוג בולט "טיוטה להתרשמות",
   כפתורי Excel/הדפסה נשארים מוצגים אך נעולים כש-`reportId` לא קיים, הודעת "זמין בגרסה המלאה
   בתשלום" בלחיצה, בלי מסלול עקיפה. פעילים כרגיל כש-`reportId` קיים (דוח נרכש).
5. **שער הרשאה לדוחות המעקב** - `app/tracking/page.tsx` נדרש לבדוק entitlement מסוג
   `trackingReports` (מהשלב 2) לפני טעינת/הצגת הנתונים, בדיוק כמו ש-`app/cashflow` (בענף הנפרד)
   בודק את שלו - לא מספיק `reportId` תקין לדוח הבסיס בלבד.
6. **חיבור דירוג הדירות לדוח פינוי-בינוי שנרכש** - `app/ranking/page.tsx` (הכלי עצמו, לא הדוגמה)
   נדרש `reportId` של דוח פינוי-בינוי **שנרכש בפועל**, לא רק `dealType` שנבחר בטופס; לא מוצג
   לסוגי עסקה אחרים; `ranking-sample` נשאר פתוח כפי שהוא, בלי שינוי.
7. **בדיקות התאמה בין הקטלוג, המסכים, התשלום וההרשאות** - שכבת בדיקות (Vitest, בדיוק כמו שאר
   הפרויקט) שמוודאת: מחיר/תכולה בכל עמוד תואמים בפועל ל-`lib/catalog.ts` (לא רק "אמורים
   להיות"), `allowedActions`/`requiresReportId` נאכפים בפועל (לא רק מתועדים), ואין נתיב עקיפה
   לכפתורי Excel/הדפסה הנעולים. **(יוזמה נפרדת, לא חלק מהרצף הזה)**: מיגרציה של `baseReport`/`custom`
   עצמם מהמודל הישן ל-entitlement המאובטח - היקף גדול משמעותית, סבב עבודה משלו.

כל commit - קוד בלבד היכן שרלוונטי, ללא Functions deploy/secrets/Cardcom אמיתי בלי אישור מפורש
נפרד, בדיוק כמו כל סבב קודם בפרויקט הזה.
