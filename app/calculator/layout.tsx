import { privatePageMetadata } from "@/lib/public-page-metadata";

export const metadata = privatePageMetadata("עריכת דוח אפס");

export default function CalculatorLayout({ children }: { children: React.ReactNode }) {
  return children;
}
