# GEN2 — תכנון כלי דירוג ובחירת יחידות (פינוי בינוי)

**סטטוס: מסמך תכנון בלבד. לא בוצע שום שינוי בכלי הדירוג הקיים, בחישובים, במסכים או בפרסום.**

נבדקו בפועל, נכון לענף `gen2-unit-ranking-design` (נפתח מ-`main`):
- `app/ranking/page.tsx`
- `app/ranking-sample/page.tsx`
- `lib/report/exportRankingExcel.ts`

כלי הדירוג היום הוא **כלי לקוח בלבד**: אין קריאה או כתיבה ל-Supabase בשום מקום בשלושת הקבצים, אין `reportId`/`?id=` בשום צורה, ואין שום שמירה. כל הנתונים חיים ב-`useState` בזיכרון הדפדפן ואובדים ברענון עמוד. זו עובדה חשובה שמשפיעה על סעיף 7 (תאימות לאחור) — אין כרגע שום דוח שמור לשבור.

---

## 1. מה בדיוק קובע את דירוג הדירה הישנה כיום

### 1.1 אימות מול הקוד בפועל

התיאור בהנחיה מדויק. הדירוג נעשה כך ([app/ranking/page.tsx:38-40](app/ranking/page.tsx#L38-L40), [202-204](app/ranking/page.tsx#L202-L204)):

```ts
function totalCoefficient(unit: UnitRow, criteria: Criterion[]): number {
  return criteria.reduce((acc, c) => acc * (unit.coefficients[c.id] ?? 1), 1);
}
// ...
const oldRanked = [...oldUnits]
  .map((u) => ({ unit: u, total: totalCoefficient(u, criteria) }))
  .sort((a, b) => b.total - a.total);
```

`basePriceNis` **אינו** משתתף בקריאה הזו כלל — הוא מגיע רק בהמשך, בחישוב "מחיר מתואם" ו"פער ערך" (סעיף 1.4). אותה פונקציה בדיוק, בשם אחר (`total`), חוזרת עוד פעמיים במקור באופן עצמאי: ב-[lib/report/exportRankingExcel.ts:15-17](lib/report/exportRankingExcel.ts#L15-L17) וב-[app/ranking-sample/page.tsx:20-22](app/ranking-sample/page.tsx#L20-L22). שלושה מימושים זהים, לא מקור אמת אחד — רלוונטי ישירות לסעיף 7.

### 1.2 שוברי שוויון / מיון משני קיימים — ותופעת לוואי נסתרת

הסימון היחיד לתיקו הוא [app/ranking/page.tsx:332](app/ranking/page.tsx#L332):

```ts
const tie = i > 0 && Math.abs(total - oldRanked[i - 1].total) < 0.0005;
```

שלוש בעיות מאומתות בקוד עצמו:

1. **`i > 0`** — האיבר הראשון בכל קבוצת תיקו אף פעם לא מסומן. אם שלוש דירות זהות בדיוק, רק השנייה והשלישית יסומנו "תיקו", לא הראשונה.
2. **השוואה לשכן הקודם בלבד**, לא לקבוצה. שלושה איברים ברצף עם הפרשים 0.0004 ו-0.0004 (סה"כ 0.0008 בין הקצוות) ייחשבו "תיקו" לפי ההשוואה הזוגית, אף שההפרש בין הראשון לאחרון עצמו חורג מהסף — שאלת עיצוב אמיתית (ר' סעיף 5.2).
3. **תופעת לוואי נסתרת**: `Array.prototype.sort` ב-JavaScript מובטח יציב (stable) מאז ES2019. כלומר, כששני איברים שווים בדיוק, `sort` **שומר על סדר ההופעה המקורי במערך** — סדר ההוספה של הדירות לטבלה. זה בפועל **שובר-תיקו סמוי שאף אחד לא בחר בו במפורש**: מי שהוזן קודם לטבלה מנצח בתיקו, בלי שום סימון או תיעוד למשתמש שזה מה שקורה. `exportRankingExcel.ts` לא מטפל בתיקו בכלל — אין שם אפילו את הסימון החלקי שקיים במסך.

### 1.3 שלוש חלופות מקצועיות, עם דוגמה מספרית

**דוגמה**: שתי דירות ישנות שונות גם במקדם וגם בשווי בסיס:

| דירה | מקדם כולל | מחיר בסיס | מחיר מתואם (מקדם×בסיס) |
|---|---|---|---|
| A (קומה גבוהה, דירה קטנה יחסית) | 1.10 | 1,800,000 ₪ | 1,980,000 ₪ |
| B (קומה נמוכה, דירה גדולה/יקרה יותר מלכתחילה) | 1.05 | 2,200,000 ₪ | 2,310,000 ₪ |

**חלופה א — מקדם יחסי בלבד (המצב הקיים)**: A מדורגת ראשונה (1.10 > 1.05). הדירוג מתעלם לחלוטין משווי הבסיס — שתי דירות בשווי בסיס שונה פי 2 עדיין יידורגו רק לפי איכות יחסית (קומה/נוף/וכו').

**חלופה ב — שווי מתואם `baseValueNis * totalCoefficient`**: B מדורגת ראשונה (2,310,000 > 1,980,000) — **סדר הפוך** מחלופה א׳. כאן דירוג התור משקף מי מחזיק בפועל בנכס בעל השווי הכולל הגבוה יותר, לא רק באיכות היחסית שלו.

**חלופה ג — שווי יחסי בתוך קבוצת הדירות הישנות**: מגדירים `relativeValue = adjustedValue / ממוצע(adjustedValue בקבוצה)`. עם שתי הדירות בלבד, הממוצע הוא 2,145,000 ₪: A ← 92.3%, B ← 107.7%. הסדר זהה לחלופה ב׳ (טרנספורמציה מונוטונית לא משנה סדר בתוך אותה קבוצה) — **חשוב להיות כנים בעניין הזה**: על קבוצה אחת, חלופה ג׳ היא בעצם חלופה ב׳ מנורמלת, לא סדר עצמאי. ג׳ נעשית משמעותית שונה מב׳ רק כאשר יש **כמה קבוצות נפרדות** (למשל כמה כניסות/אגפים בבניין הישן, שכל אחד מקבל הקצאה נפרדת מדירות חדשות) — ואז ההחלטה איזו אוכלוסייה לנרמל מולה (הבניין כולו? האגף בלבד?) היא **החלטה מקצועית בפני עצמה**, לא טכנית (ר' רשימת ההחלטות הדורשות אישור, בסוף המסמך).

**חשוב — אין לאמץ עדיין אף חלופה אחרת כברירת מחדל.** דירוג לצורך **סדר בחירת דירות** אינו בהכרח זהה לדירוג **שווי הנכסים הקיימים**: מעבר משקוף מ-`totalCoefficient` ל-`baseValueNis * totalCoefficient` עלול לתת עדיפות קבועה לבעלי דירות גדולות/יקרות יותר, גם כשההסכם קובע זכאות תמורה אחרת לגמרי או סדר בחירה לפי איכות יחסית בלבד (חלופה א׳ בכוונה מכוונת, לא מגבלה טכנית). עד לקבלת מקור מקצועי או חוזי ספציפי לפרויקט, שלוש החלופות מתועדות כאן כטיפוס מפורש וניתן לבחירה, **לא כדירוג מועדף מרומז**:

```ts
type RankingMethod =
  | "legacyCoefficientOnly"        // חלופה א - המצב הקיים, ברירת המחדל
  | "adjustedOldApartmentValue"    // חלופה ב - baseValueNis * totalCoefficient
  | "relativeWithinGroup";         // חלופה ג - שווי יחסי בתוך קבוצה
```

`RankingMethod` הוא קלט מפורש למנוע (ר' `RankingAssumptions` בסעיף 7), לא קבוע קשיח. `legacyCoefficientOnly` נשאר ברירת המחדל עד החלטה מקצועית נפרדת — ר' "החלטות תכנון מאושרות" בהמשך המסמך. שינוי ברירת המחדל בשקט, בלי בחירה מפורשת של המשתמש, אסור.

### 1.4 תפקידו הנוכחי של `basePriceNis`

מאומת: **לא משתתף בדירוג**. תפקידו היחיד היום: קלט לחישוב "מחיר מתואם" בתצוגה (`basePriceNis * total`) ול"פער ערך" בסעיף הבחירה (סעיף 2). השם `basePriceNis` מטעה — הוא נשמע כאילו הוא בסיס לחישוב הדירוג עצמו, בעוד בפועל הוא בסיס לחישוב *ערך כספי בלבד*, נפרד לגמרי מסדר התור. מוצע: לשנות שם לשדה חדש (במנוע המוצע, סעיף 7) ל-`baseValueNis`, ולתעד במפורש בממשק (טקסט קבוע ליד השדה) שהוא **אינו** משפיע על סדר הבחירה בברירת המחדל (חלופה א׳) — ואם וכאשר תאושר חלופה ב׳ או ג׳, השם `baseValueNis` כבר יתאים סמנטית בלי שינוי נוסף.

---

## 2. הפרדת שווי דירה ישנה מזכאות לדירת תמורה

### 2.1 אימות מול הקוד

מדויק. פער הערך מחושב **בשני מקומות עצמאיים** (לא מקור אמת יחיד — שוב רלוונטי לסעיף 7):

- מסך, [app/ranking/page.tsx:337-340](app/ranking/page.tsx#L337-L340):
  ```ts
  const valueGap = chosenUnit.basePriceNis * chosenTotal! - unit.basePriceNis * total;
  ```
- Excel, [lib/report/exportRankingExcel.ts:61-62](lib/report/exportRankingExcel.ts#L61-L62):
  ```ts
  const valueGap = chosenUnit ? round(chosenUnit.basePriceNis * chosenTotal - unit.basePriceNis * t) : "";
  ```

זו אכן `adjustedNewValue - adjustedOldValue` — **לא** השוואה מקצועית נכונה במלואה לצורך תשלום איזון. הבעיה: `unit.basePriceNis * total` הוא שווי הדירה הישנה *כפי שהוערכה*, לא בהכרח **זכאות התמורה** החוזית של הדייר. בפינוי בינוי, נוסחת הזכאות לרוב כוללת מרכיבים חוזיים נפרדים (בונוס פינוי, אחוז תוספת שטח מוסכם, וכו') שעשויים להיות **שונים** משווי השמאות הגולמי של הדירה הישנה. הקוד הקיים מניח בשקט ששני הדברים זהים — הנחה שעלולה להיות שגויה בפרויקטים עם נוסחת זכאות מיוחדת.

### 2.2 מודל מוצע — הפרדת ארבעה ערכים

```ts
oldApartmentValueNis          // שווי הדירה הישנה כפי שהוערכה (מקדם × מחיר בסיס) - נתון שמאי גרידא
entitlementValueNis           // שווי הזכאות החוזית לתמורה - עשוי להיות שונה מ-oldApartmentValueNis
selectedNewApartmentValueNis  // שווי הדירה החדשה שהדייר בפועל בחר
balancePaymentNis             // = selectedNewApartmentValueNis - entitlementValueNis
```

**המפתח**: `balancePaymentNis` מחושב מול `entitlementValueNis`, **לא** מול `oldApartmentValueNis`. בברירת מחדל, כשאין נוסחת זכאות מיוחדת מוזנת, `entitlementValueNis` יכול להיות שווה ל-`oldApartmentValueNis` (זהה למצב היום) — אבל זה חייב להיות בחירה מפורשת של המשתמש/שמאי, לא הנחה שתקועה בקוד. **אין לקבוע שדייר חייב בתשלום רק כי הדירה החדשה שווה יותר מדירתו הישנה** — הקביעה חייבת לעבור דרך הזכאות, לא דרך שווי הדירה הישנה הגולמי.

### 2.3 ייצוג זיכוי מול תשלום

`balancePaymentNis` שלילי משמעו זיכוי לדייר, לא "חוב שלילי". מוצע שדה נגזר מפורש כדי שלא יישאר עמום למי שקורא רק מספר:

```ts
type BalanceDirection = "residentPays" | "residentCredited" | "none";
```

`"none"` כאשר ההפרש בתוך סף עיגול/תיקו זניח (לא 0 בדיוק בהכרח — ר' סף התיקו העשרוני בסעיף 5.2, אותה שאלת סף חוזרת כאן). המסך הקיים כבר מציג צבע אדום לפער שלילי ומשפט הסבר ("פער שלילי... ייתכן תשלום איזון לדייר") — הניסוח קצת מגושם (קורא ל"זיכוי" בשם "תשלום") ומוצע לחדד ללשון "זיכוי" מפורשת, לא רק לצבוע אדום.

---

## 3. מקדמים ומשקולות

המודל הכפלי הקיים (`reduce((acc,c)=>acc*coef,1)`) מוצדק כאשר הקריטריונים מבטאים **אפקטים יחסיים/אחוזיים בלתי-תלויים** — טבעי לתחום השמאות שבו כל תכונה בדרך כלל "מוסיפה אחוז" למחיר הבסיס, לא סכום קבוע. הבעיה: מכפלה מניחה **אי-תלות מלאה** בין הקריטריונים.

חלופה להשוואה — **מודל ציונים משוקלל**:

```
score = Σ (criterionScore_i * weight_i) / Σ weight_i
```

כאשר `criterionScore` מנורמל לסולם קבוע (למשל ‑1..+1), ו-`weight` מבטא חשיבות יחסית מפורשת לכל קריטריון (בניגוד למודל הכפלי, שבו "חשיבות" קריטריון מיוצגת רק במרומז דרך גודל סטיית המקדם מ-1, לא כפרמטר נפרד).

תשובות לנקודות שהתבקשו:

- **מתי מכפלה מוצדקת**: כשהקריטריונים באמת בלתי-תלויים ומבטאים אפקט אחוזי (המקרה הרגיל בשמאות). כשלים: שני מקדמים קיצוניים (1.15 ו-0.85) יחד יוצרים אפקט "מרוכך" (0.9775) שאולי לא נועד — תוצר הכפל, לא סכום, לא תמיד תואם אינטואיציה.
- **תוספת קריטריון**: קריטריון חדש קרוב ל-1 כמעט לא משנה סדר; קריטריון קיצוני (1.3) יכול לשנות מהותית את הסדר גם אם לא נועד לכך, כי אין נורמליזציה אוטומטית של "כמה קריטריונים כבר יש".
- **ספירה כפולה**: נוף/קומה/מספר חזיתות עשויים לתאר בפועל את אותה תכונה (קומה גבוהה ⇒ נוף טוב ⇒ חזית פתוחה). קריטריונים קורלטיביים "מכפילים" את אותו אפקט כמה פעמים בשקט. מוצע: קיבוץ קריטריונים למשפחות בלעדיות (קריטריון מייצג אחד למשפחה), או תיעוד מפורש שבדיקת אי-חפיפה היא באחריות השמאי המזין.
- **כיול**: להגביל טווח מומלץ לכל מקדם (למשל 0.85–1.15) עם **אזהרה** בחריגה (לא חסימה — ר' סעיף 6), ולהציג את טווח האפקט הכולל הריאלי (למשל 0.7–1.4 עבור 5 קריטריונים) כדי שהשמאי יראה אם המכפלה יצאה קיצונית.
- **סדר קריטריונים**: מתמטית לא משנה (כפל קומוטטיבי) — משפיע רק על סדר התצוגה בטבלה.
- **הסבר הניתן לביקורת**: מוצע פירוט "breakdown" שורה-שורה לכל דירה — כל קריטריון והשפעתו הבודדת ("קומה: ×0.97 (-3%)"), לא רק התוצאה הסופית — כך שכל דירוג ניתן לשחזור ולערעור נקודתי.

**לא משנים את המודל כעת**. ההמלצה: להשאיר את המודל הכפלי כברירת המחדל (הוא כבר תואם את הפרקטיקה השמאית המתוארת ואת מה שהשמאים מכירים), ולתעד את הרחבת "משקל מפורש" כאפשרות עתידית הדורשת החלטה מקצועית נפרדת — לא להחליט כעת.

---

## 4. בלעדיות בבחירת דירות חדשות

### 4.1 בדיקה בפועל

**מאומת: כן, אותה דירה חדשה יכולה להיבחר היום עבור כמה דיירים.** ה-`<select>` ב-[app/ranking/page.tsx:352-364](app/ranking/page.tsx#L352-L364) מציג את **כל** `newUnits` בלי שום סינון של מה שכבר נבחר:

```tsx
<select value={chosenId} onChange={...}>
  <option value="">— טרם נבחר —</option>
  {newUnits.map((nu) => <option key={nu.id} value={nu.id}>{nu.name || "ללא שם"}</option>)}
</select>
```

אין שום בדיקת כפילות גם בייצוא ה-Excel (`buildRankingWorkbook` מקבל `choices` כמו שהוא, בלי אימות).

**נקודה חיובית שכדאי לציין באמת**: אחסון הבחירה (`choices: Record<string,string>`, ב-[app/ranking/page.tsx:158](app/ranking/page.tsx#L158)) **כבר** משתמש במזהים יציבים (`unit.id`, לא בשם תצוגה ולא במיקום במערך) — הדרישה "שמירת בחירה באמצעות מזהה יציב" **כבר מתקיימת היום**, אין צורך לתקן את זה, רק את כלל הבלעדיות עצמו.

### 4.2 תכנון

- **חסימת בחירה כפולה כברירת מחדל**: אפשרות שכבר נבחרה על ידי דייר אחר מוצגת ב-`<option disabled>`, לא מוסתרת (כדי שיהיה ברור שהיא קיימת אך תפוסה).
- **"כבר נבחרה על ידי…"**: טקסט בתוך התווית של האופציה החסומה (`{nu.name} — כבר נבחרה ע"י {residentName}`), לא רק חסימה שקטה.
- **ביטול/שינוי בחירה**: פעולת "בטל בחירה" מפורשת. העברת בחירה מדייר לדייר: לשחרר קודם באופן מפורש, ולא לאפשר "גניבה" שקטה של בחירה קיימת בבחירה חדשה — דורש אישור עיצוב (ר' רשימת החלטות).
- **אזהרה כשדירה שנבחרה נמחקה**: כרגע מחיקת `newUnit` שכבר נבחרה משאירה `chosenId` תלוי-באוויר בשקט — `newUnitsById.get(chosenId)` מחזיר `undefined`, והעמודות פשוט מציגות "-" בלי שום הודעה. מוצע: באנר אזהרה מפורש בשם הדייר/הדירה הישנה שנפגעו.
- **בדיקת שלמות**: תצוגת סיכום — X מתוך Y דיירים שובצו, Y-X דירות חדשות עדיין פנויות, רשימת התנגשויות אם יש.
- **יותר דיירים מדירות**: מצב מפורש וגלוי (לא שקט) — באנר "X דיירים, Y דירות בלבד — נדרשת החלטה נוספת".
- **מזהה יציב**: כאמור, כבר מתקיים היום.

---

## 5. תיקו

### 5.1 בדיקה בפועל

ר' סעיף 1.2 — שלוש הבעיות המאומתות: האיבר הראשון בקבוצה לא מסומן, השוואה זוגית בלבד (לא קבוצתית), ו-Excel לא מציג תיקו כלל.

### 5.2 עיצוב מוצע

- **קבוצות תיקו, לא רק דגל בוליאני**: כל דירה מקבלת `tieGroupId` (או `null`). כל חברי הקבוצה מסומנים — כולל הראשון.

- **שוויון מדויק (`===`) אינו מתאים לתוצאת מכפלה.** גם כשכל מקדם הוזן כמספר "עגול" (למשל 1.0, 0.95, 1.05), מכפלת כמה מספרים עשרוניים יכולה לייצר שאריות floating-point זעירות (בדיוק אותה משפחת בעיה שתועדה ותוקנה ב-commit 5b של מנוע התזרים — ר' `MONEY_EPSILON_NIS`/`normalizeMoney` ב-`lib/calc/cashflow-interest-engine.ts`). שתי דירות עם אותם מקדמים "בכוונה" עלולות לצאת עם `totalCoefficient` שונה ב-1e-15 ולא להיתפס כתיקו למרות שהתכוונו לזהות מדויקת. **אין לקבוע תיקו לפי `===` על התוצאה הגולמית.** מדיניות התיקו חייבת להיות הנחת-יסוד גלויה של הכלי, לא קבוע סמוי:

  ```ts
  interface TiePolicy {
    mode: "roundedScore" | "absoluteTolerance";
    displayedDecimals?: number;  // עבור roundedScore - כמה ספרות אחרי הנקודה נחשבות ל"אותו ציון"
    tolerance?: number;          // עבור absoluteTolerance - סף מוחלט
    grouping: "anchored" | "transitive";
  }
  ```

  אין נקבע כאן ערך מקצועי סופי ל-`tolerance`/`displayedDecimals` — זו החלטה נפרדת (ר' "החלטות מקצועיות שנותרו פתוחות" בסוף המסמך).

- **קיבוץ: `anchored` מוצע כברירת מחדל, לא `transitive`.** בשרשור טרנזיטיבי עלול להיווצר מצב שבו A קרובה ל-B וגם B קרובה ל-C, ולכן שלושתן מקובצות יחד, אף ש-A ו-C עצמן רחוקות מעבר לסף. דוגמה מספרית עם סף `0.0005`:

  | דירה | ציון (`totalCoefficient`) |
  |---|---|
  | A | 1.0000 |
  | B | 1.0004 |
  | C | 1.0008 |

  `|B-A| = 0.0004` (בתוך הסף), `|C-B| = 0.0004` (בתוך הסף), אבל `|C-A| = 0.0008` (**מעבר** לסף). **`transitive`**: A,B,C בקבוצה אחת אחת (שרשור A→B→C). **`anchored`** (מוצע כברירת מחדל): כל חבר בקבוצה חייב להיות בתוך הסף **מהציון הראשון/הגבוה בקבוצה** (כאן A) — כך B מצטרפת ל-A, אך C **לא** (`|C-A|>0.0005`), ונשארת קבוצה נפרדת (או בודדת). `transitive` נשאר אפשרות מפורשת נתמכת, לא ברירת מחדל.

- **תצוגה נדרשת**, גם במסך וגם ב-Excel (כרגע קיים חלקית רק במסך): מספר/מזהה קבוצת התיקו, רשימת הדירות המשתתפות, הערה "נדרשת הכרעה או הגרלה חיצונית".
- **לא לשבור תיקו אוטומטית** לפי סדר הזנה/אינדקס/מזהה — כפי שקורה היום *בשקט* דרך יציבות ה-`sort` (סעיף 1.2). זו בדיוק ההתנהגות שיש להימנע ממנה במפורש במימוש החדש.

---

## 6. ולידציה

חלוקה מפורשת בין **שגיאות חוסמות** (מונעות שמירה/ייצוא) לבין **אזהרות מקצועיות** (מוצגות אך לא חוסמות):

**שגיאות חוסמות:**
- שווי בסיס (`baseValueNis`) שלילי.
- מקדם שאינו מספר סופי, או מקדם ≤ 0 (מקדם 0 מאפס בשקט את כל המכפלה — כנראה טעות הזנה, לא ערך לגיטימי).
- דירה בלי מזהה יציב (כבר מובטח היום ע"י `nextId`, לתעד כדרישה ולא רק כמקרה).
- **שם דירה ריק.**
- **שם קריטריון ריק.**
- **שם קריטריון כפול לאחר נרמול** (`trim()` והשוואה לא רגישה לרישיות) — היום זו רק אזהרה מתוכננת; הועבר לחוסם כי שני קריטריונים בשם זהה מטעים בתצוגה ובייצוא באופן שלא ניתן לתקן בדיעבד מתוך הדוח עצמו.
- **כלי ללא קריטריונים כלל.**
- **כלי ללא דירות ישנות כלל** (בלי דירות ישנות אין מה לדרג — שונה מ"אין דירות חדשות", שעדיין עשוי להיות מצב ביניים לגיטימי לפני שקטלוג הדירות החדשות הושלם).
- **ניסיון ייצוא כשאין תוצאה תקינה** (כשיש `blockingErrors`) — הייצוא עצמו נחסם, לא רק "מוזהר עליו".
- כל `NaN`/`Infinity` בפלט המנוע.
- בחירה של `newUnitId` שלא קיים ברשימת הדירות החדשות (שלמות רפרנציאלית).
- בחירה כפולה של אותה דירה חדשה (ר' סעיף 4).
- **שווי בסיס `undefined` (טרם הוזן) כשה-`RankingMethod` הנבחר משתמש בשווי** (`adjustedOldApartmentValue`/`relativeWithinGroup`) — חוסם, כי הדירוג עצמו תלוי בערך חסר. תחת `legacyCoefficientOnly` (ברירת המחדל) `undefined` אינו חוסם, רק אזהרה (ר' למטה) — כי השווי כלל לא משתתף בדירוג תחת השיטה הזו.
- **שווי בסיס `0` כשה-`RankingMethod` משתמש בשווי, אלא אם סומן במפורש כערך אפס אמיתי** (לא חסר) — ר' ההבחנה `undefined`/`0` למטה.

**אזהרות (לא חוסמות):**
- מקדם מחוץ לטווח המומלץ (למשל 0.85–1.15, ר' הערה בסעיף 3/7 שהטווח עצמו הוא קלט, לא קבוע) — מודגש בענבר, לא נחסם (ערכים קיצוניים מכוונים עדיין אפשריים ולגיטימיים).
- **שווי בסיס `0` או `undefined` תחת `legacyCoefficientOnly`** — אזהרה "שווי בסיס חסר", לא חוסם, כי השווי לא משתתף בדירוג עצמו תחת השיטה הזו, רק בתצוגת "מחיר מתואם"/פער הערך שבסעיף 2.
- מחיקת דירה/קריטריון שכבר נבחר/בשימוש — התנהגות הקיימת החלק היחיד שכבר תקין: מחיקת קריטריון **כן** מנקה כראוי את הערכים היתומים מכל היחידות ([app/ranking/page.tsx:169-178](app/ranking/page.tsx#L169-L178), `stripCoef` באמצעות destructuring) — **אין באג כאן, ראוי לציין זאת בפירוש כדי לא "לתקן" משהו שכבר עובד**. לעומת זאת מחיקת דירה שנבחרה **כן** צריכה אזהרה גלויה (ר' סעיף 4.2) שאינה קיימת היום.

**הבחנה מפורשת בין ערך חסר לערך אפס אמיתי** (רלוונטי בעיקר ל-`baseValueNis`, אך אותו עיקרון חל על כל שדה כספי אחר במנוע): `undefined` = טרם הוזן; `0` = ערך שהוזן במפורש. **אין להשתמש ב-`0` כברירת מחדל שמשמעותה גם "חסר"** — ר' `RankingUnit` המעודכן בסעיף 7 (`baseValueNis?: number` + `baseValueConfirmedZero?: boolean`).

**עקרון עיצוב, לא רק בדיקה בודדת — מסך ו-Excel כתלות, לא כמחשבים עצמאיים**: `computeRanking` מחזיר `RankingResult` יחיד; `app/ranking/page.tsx` ו-`lib/report/exportRankingExcel.ts` מקבלים אותו **כקלט** ומרנדרים בלבד — **אין להם הרשאה לחשב מחדש** דירוג, קבוצת תיקו, או תשלום איזון בעצמם. בדיקות ה-Excel (ר' סעיף 8, פריט 13) מאמתות שכל שורה ושדה שיוצאו תואמים בערכם ל-`RankingResult` שחושב — לא רק ש"שני הצדדים לא זרקו שגיאה" וגם לא "שניהם מחזיקים את אותו object reference" (דרישה לא מעשית כשה-Excel נכתב ל-XLSX ונקרא חזרה כערכים גולמיים). כרגע יש **שלושה** מימושים עצמאיים של נוסחת הדירוג (סעיף 1.1) ו**שני** מימושים עצמאיים של פער הערך (סעיף 2.1) — בדיוק הפער שגרם לבעיות דומות בעבר במנוע התזרים (`gen2-cashflow-engine`) לפני שהופרד למודול משותף.

---

## 7. מבנה נתונים ומנוע נפרד

מוצע להעביר טיפוסים וחישובים טהורים מתוך `app/ranking/page.tsx` למודול נפרד, `lib/calc/ranking-engine.ts`, באותו דפוס שהוקם ב-`lib/calc/cashflow-*.ts` (טהור, דטרמיניסטי, בלי מוטציה, ניתן לבדיקה אוטומטית, בלי תלות ב-React/Supabase).

```ts
interface RankingCriterion {
  id: string;                 // uuid יציב, לא תלוי בסדר/שם
  name: string;
}

interface RankingUnit {
  id: string;                          // uuid יציב
  name: string;
  group: "old" | "new";                // מחליף את ההפרדה הקשיחה ל-oldUnits/newUnits בשני state נפרדים
  coefficients: Record<string, number>; // criterionId -> מקדם
  baseValueNis?: number;                // היה basePriceNis (סעיף 1.4). undefined = טרם הוזן (סעיף 6)
  baseValueConfirmedZero?: boolean;     // true = 0 הוזן במפורש כערך אמיתי, לא "חסר" (סעיף 6)
}

type RankingMethod =
  | "legacyCoefficientOnly"        // ברירת המחדל - ר' סעיף 1.3 ו"החלטות תכנון מאושרות"
  | "adjustedOldApartmentValue"
  | "relativeWithinGroup";

interface TiePolicy {
  mode: "roundedScore" | "absoluteTolerance";
  displayedDecimals?: number;
  tolerance?: number;
  grouping: "anchored" | "transitive";  // "anchored" מוצע כברירת מחדל - ר' סעיף 5.2
}

interface RankingAssumptions {
  rankingMethod: RankingMethod;
  tiePolicy: TiePolicy;
  recommendedCoefficientRange?: { min: number; max: number }; // קלט/הגדרה, לא קבוע קשיח (סעיף 3/6)
}

interface ResidentEntitlement {
  id: string;
  oldUnitId: string;           // RankingUnit עם group="old"
  entitlementValueNis: number; // שווי הזכאות החוזית - נפרד מ-oldApartmentValueNis (סעיף 2)
}

interface UnitSelection {
  id: string;
  residentEntitlementId: string;
  newUnitId: string;           // RankingUnit עם group="new"
}

type BalanceDirection = "residentPays" | "residentCredited" | "none";

interface RankedResident {
  residentEntitlementId: string;
  oldUnitId: string;
  rankPosition: number;              // תור, 1-based
  totalCoefficient: number;
  tieGroupId: string | null;
  selection: {
    newUnitId: string;
    newTotalCoefficient: number;
    selectedNewApartmentValueNis: number;
  } | null;                          // null אם טרם נבחר
  balancePaymentNis: number | null;
  balanceDirection: BalanceDirection | null;
}

interface TieGroup {
  id: string;
  groupNumber: number;               // 1-based, לתצוגה
  memberOldUnitIds: string[];        // כולל את הראשון בקבוצה (סעיף 5)
  totalCoefficient: number;
  requiresExternalDecision: true;
}

interface RankingValidationIssue {
  code: string;                      // מזהה יציב לבדיקה אוטומטית, למשל "DUPLICATE_SELECTION"
  message: string;
  relatedUnitIds?: string[];
}

interface RankingValidationResult {
  blockingErrors: RankingValidationIssue[];
  warnings: RankingValidationIssue[];
}

interface RankingResult {
  schemaVersion: number;
  rankingMethod: RankingMethod;      // תיעוד איזו שיטה בפועל חושבה, לצורך מעקב/ביקורת
  rankedResidents: RankedResident[];
  tieGroups: TieGroup[];
  validation: RankingValidationResult;
}
```

`computeRanking(criteria, units, entitlements, selections, assumptions: RankingAssumptions): RankingResult` — פונקציה טהורה אחת. גם `app/ranking/page.tsx` וגם `lib/report/exportRankingExcel.ts` מקבלים את הפלט שלה **כתלות** ומרנדרים אותו, בלי הרשאה לחשב דבר בעצמם (ר' עיקרון "מסך ו-Excel כתלות" בסעיף 6).

**תאימות לאחור / `schemaVersion`**: כאמור בפתיח, **אין כיום שום דוח שמור** — הכלי כולו client-side ללא Supabase. לכן אין כרגע שום דבר קיים שעלול להישבר. עדיין מוצע `schemaVersion` במבנה `RankingResult` כבר עכשיו, באופן יזום, למקרה שהכלי יחובר לשמירה בעתיד (בדומה לדוח המעקב שנוסף החודש) — כדי שתהיה נקודת עיגון נקייה למיגרציה עתידית, לא כי יש היום מה לתאם.

---

## 8. בדיקות

תוכנית בדיקות מוצעת ל-`lib/calc/ranking-engine.test.ts`, באותו דפוס vitest שהוקם השבוע (`cashflow-*.test.ts`):

1. דירוג עם מחירי בסיס שונים תחת `legacyCoefficientOnly` — מוודא ש-`baseValueNis` לא משפיע על הדירוג. בדיקה נפרדת תחת `adjustedOldApartmentValue`/`relativeWithinGroup` — מוודאת שכן משפיע (ולפי הנוסחה הנכונה לכל שיטה).
2. שינוי מספר הקריטריונים (הוספה/הסרה) לא משאיר ערכים יתומים ולא שובר את חישוב שאר הדירות.
3. מקדם אפס — שגיאה חוסמת. מקדם שלילי — שגיאה חוסמת. מקדם לא סופי (`NaN`/`Infinity`) — שגיאה חוסמת. מקדם קיצוני (מחוץ לטווח מומלץ) — אזהרה בלבד, לא חוסם.
3א. `baseValueNis` — `undefined` מול `0` מפורש (`baseValueConfirmedZero`) נבדקים כשני מצבים נפרדים, לא זהים: תחת `legacyCoefficientOnly` שניהם אזהרה בלבד; תחת שיטה מבוססת-שווי `undefined` וגם `0` לא-מאושר חוסמים, אך `0` עם `baseValueConfirmedZero===true` **לא** חוסם.
4. תיקו של שני דיירים — שניהם מסומנים, כולל הראשון.
5. תיקו של שלושה דיירים במבנה A/B/C מהדוגמה בסעיף 5.2 (סף 0.0005, הפרשים 0.0004/0.0004/0.0008 מקצה לקצה) — תחת `grouping:"anchored"` A ו-B בקבוצה אחת, C נפרדת; תחת `grouping:"transitive"` שלושתן קבוצה אחת. שתי הבדיקות רצות זו לצד זו כדי שההבדל בין המדיניות יהיה מתועד ולא רק תיאורטי.
6. כמה קבוצות תיקו נפרדות בו-זמנית ברשימה אחת — כל קבוצה מקבלת `tieGroupId` נפרד ונכון.
7. בחירה כפולה של אותה דירה חדשה — שגיאה חוסמת, לא מתעלמת בשקט.
8. מחיקת דירה חדשה שכבר נבחרה — מייצרת אזהרה גלויה עם שם הדייר המושפע, לא רק "-" שקט.
9. זכאות תמורה שונה משווי הדירה הישנה — `balancePaymentNis` מחושב מול `entitlementValueNis`, לא מול `oldApartmentValueNis` (מקרה בו הם שונים בכוונה).
10. מצב זיכוי לדייר — `balanceDirection==="residentCredited"` כש-`selectedNewApartmentValueNis < entitlementValueNis`.
11. מספר דיירים גדול ממספר הדירות הזמינות — מצב "לא כולם ישובצו" מסומן במפורש, לא רק חוסר שקט.
12. שמות קריטריונים כפולים (אחרי `trim` + השוואה לא רגישה לרישיות) — **שגיאה חוסמת** (עודכן מאזהרה, ר' סעיף 6). בדיקה נפרדת: שם דירה ריק — חוסם. שם קריטריון ריק — חוסם. כלי ללא קריטריונים — חוסם. כלי ללא דירות ישנות — חוסם.
13. התאמה מלאה בין תוצאת המסך לתוצאת ה-Excel — נבדק כ"שורות ה-Excel שיוצאו תואמות בערכן לשדות ה-`RankingResult` שחושב" (כולל `tieGroupId`/קבוצות תיקו, זכאות, `balancePaymentNis`, `balanceDirection`), **לא** "אותו object reference" (לא מעשי אחרי כתיבה/קריאה חוזרת מ-XLSX) וגם לא רק "שני הצדדים לא זרקו שגיאה".
14. יציבות התוצאה כשסדר הקלט משתנה — דירוג התוצאה (`rankPosition`, `tieGroupId`) זהה גם אם מערך `units` הקלט מגיע בסדר אחר (למעט שובר-התיקו הסמוי הקיים היום שדווקא אמור *להיעלם* בעיצוב החדש — ר' סעיף 1.2/5.2).
15. אי-מוטציה של הקלט — `criteria`/`units`/`entitlements`/`selections`/`assumptions` הנכנסים לא משתנים אחרי הקריאה (אותו דפוס בדיקה כמו ב-`cashflow-interest-engine.test.ts`).
16. תאימות לנתונים קיימים — נבדק כ"אין מה לבדוק כרגע" (סעיף 7), עם הערה מפורשת בבדיקה שמסבירה למה (כדי שלא ייראה כמו כיסוי חסר בטעות בעתיד).
17. כלי ריק/חלקי (0 קריטריונים או 0 דירות ישנות) — `computeRanking` מחזיר `blockingErrors` במקום תוצאה שנראית תקינה; ה-Excel לא מיוצא כשיש שגיאות חוסמות.
18. שינוי `RankingMethod` בין הרצות זהות בשאר הקלט — מוודא שרק `rankingMethod`/`rankingScore`/`balancePaymentNis` (לפי הרלוונטי) משתנים, ולא שדות בלתי-קשורים כמו `entitlementValueNis` עצמו — מוודא הפרדה אמיתית בין `rankingScore`, שווי הדירה הישנה, ו-`entitlementValueNis` (ר' "החלטות תכנון מאושרות", פריט 2).

---

## תוכנית יישום מוצעת (commits קטנים, לביצוע רק לאחר אישור)

1. `lib/calc/ranking-engine.ts` + טיפוסים בלבד, בלי חיבור למסכים — מקביל ל-commit 1 של מנוע התזרים.
2. `computeRanking` — דירוג + זיהוי קבוצות תיקו, בלי בחירה/זכאות/ולידציה עדיין.
3. הרחבה: `ResidentEntitlement`, `UnitSelection`, חישוב `balancePaymentNis`/`balanceDirection`.
4. `RankingValidationResult` — שגיאות חוסמות ואזהרות, נפרד מהחישוב עצמו.
5. חיבור `app/ranking/page.tsx` למנוע (מחליף את הלוגיקה המקומית הקיימת, לא מוסיף לוגיקה מקבילה).
6. חיבור `lib/report/exportRankingExcel.ts` לאותה תוצאה בדיוק (מקור אמת יחיד).
7. `app/ranking-sample/page.tsx` — עדכון לשימוש באותו מנוע, לא מימוש רביעי עצמאי.

כל commit: `npm test` + `npm run build` + `npm run lint`, נסקר לפני המשך, בדיוק כמו תהליך העבודה שהוקם במנוע התזרים.

---

## החלטות תכנון מאושרות (עד אימות מקצועי נוסף)

1. **ברירת המחדל נשארת `legacyCoefficientOnly`.** `adjustedOldApartmentValue`/`relativeWithinGroup` זמינות כבחירה מפורשת דרך `RankingAssumptions.rankingMethod`, לא כברירת מחדל שקטה (ר' סעיף 1.3).
2. **שווי ישן, זכאות, ושווי הדירה הנבחרת הם שדות נפרדים** — `oldApartmentValueNis` / `entitlementValueNis` / `selectedNewApartmentValueNis` (ר' סעיף 2.2), ו-`rankingScore` (התוצאה של `totalCoefficient` או של שיטת הדירוג הנבחרת) מופרד לחלוטין משלושתם. שינוי אחד לא גורר שינוי סמוי באחר.
3. **המודל הכפלי (מקדמים סביב 1) נשאר בשלב הראשון.** משקולות מפורשות (סעיף 3, מודל ציונים משוקלל) הן הרחבה עתידית אפשרית, לא חלק מ-commit 1.
4. **מדיניות תיקו מפורשת ומתועדת (`TiePolicy`), לא `===` על התוצאה הגולמית** (ר' סעיף 5.2). ערכי ברירת המחדל בפועל (`tolerance`/`displayedDecimals`) עדיין לא נקבעו — נשארים בסעיף "החלטות הדורשות אישור" למטה.
5. **ברירת המחדל המוצעת לקיבוץ תיקו היא `anchored`, לא `transitive`** (ר' דוגמת A/B/C בסעיף 5.2). `transitive` נתמך כאפשרות מפורשת.
6. **העברת דירה שנבחרה מחייבת שחרור מפורש; אין "גניבה" אוטומטית** מדייר קודם בלי פעולה נפרדת (ר' סעיף 4.2).
7. **טווח מקדמים מומלץ הוא קלט/הגדרה (`RankingAssumptions.recommendedCoefficientRange`), לא קבוע קשיח בקוד.** הערך המספרי עצמו (למשל 0.85–1.15) נשאר דוגמה בלבד, לא ערך מקצועי מאושר.
8. **אין להתחיל יישום לפני החלטה נפרדת כיצד ייקבע `entitlementValueNis` בפועל** בכל פרויקט — נוסחה חוזית, קלט ידני, או ברירת מחדל שווה ל-`oldApartmentValueNis`. זו החלטה מקדימה לתוכנית היישום (סעיף לעיל), לא חלק ממנה.

---

## החלטות מקצועיות שנותרו פתוחות (לא הוכרעו במסמך הזה)

1. **קיבוץ לחלופה ג׳** (`relativeWithinGroup`): אם וכאשר תיבחר — מהי הקבוצה לנרמול? הבניין כולו, או כל אגף/כניסה בנפרד? (ר' סעיף 1.3).
2. **ערכי ברירת המחדל של `TiePolicy`**: `mode` מומלץ (`roundedScore`/`absoluteTolerance`), `tolerance`/`displayedDecimals` בפועל — לא נקבעו כאן במכוון (ר' החלטה מאושרת #4 לעיל).
3. **הערך המספרי של טווח המקדם המומלץ** לכל קריטריון/פרויקט — המנגנון (קלט, לא קבוע) הוחלט, הערך עצמו לא (ר' החלטה מאושרת #7).
4. **נוסחת `entitlementValueNis`** בפועל לפרויקט נתון — ר' החלטה מאושרת #8: נדרשת החלטה נפרדת לפני תחילת יישום.

---

## `diff` — קובץ זה בלבד

מסמך זה הוא קובץ חדש (`git add` בלבד, אין שינוי לשום קובץ אחר). ה-diff המלא מוצג בפלט ה-commit הבא, בענף `gen2-unit-ranking-design`, נפרד לחלוטין מ-`gen2-cashflow-engine`.
